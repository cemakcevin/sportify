import './Loading.scss';

function Loading () {

    return (
        <main className="loading">
            <h1 className="loading__title">Loading...</h1>
        </main>
    )
}

export default Loading;